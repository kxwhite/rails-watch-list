<%= simple_form_for @list do |l| %>
  <%= l.input :name, placeholder: 'Horror' %>
  <%= l.input :photo %>
  <%= l.submit '', class: 'btn btn-primary mb-2' %>
<% end %>

<%= link_to 'Back', lists_path %>
