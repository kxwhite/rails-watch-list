<%= simple_form_for [ @list, @bookmark ] do |f| %>
  <%= f.input :comment, placeholder: "What did you think about the movie?" %>
  <%# <%= f.association :list %>
  <%# <%= f.association :list, as: :check_boxes %>
  <%= f.association :movie %>
  <%= f.submit 'Add movie', class: 'btn btn-primary mb-2'%>
<% end %>
